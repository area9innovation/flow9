export {
	Jingo<?> ::= JOperand, JValue<?>;

	JOperand(op : string);

	JValue<?> ::= JInt, JString, JArray<?>, JNative<?>;
		JInt(val : int);
		JString(s : string);
		JArray(vals : [Jingo<?>]);
		JNative(value : ?);
}
