export {
	TType ::= TTypeBool, TTypeInt, TTypeString, TTypeArray, TTypeFunction, TTypeVar;
		TTypeBool();
		TTypeInt();
		TTypeString();
		TTypeArray(type : TType);
		TTypeFunction(args : [TType], returnType : TType);

		TTypeVar(id : int);
}
