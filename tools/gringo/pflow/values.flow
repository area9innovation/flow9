import tools/gringo/pflow/pexp;

export {
	getPString(e : PExp) -> string;
	getPArray(e : PExp) -> [PExp];
}

getPString(e : PExp) -> string {
	switch (e) {
		PString(s): s;
		default: "";
	}
}

getPArray(e : PExp) -> [PExp] {
	switch (e) {
		PArray(es): es;
		default: [];
	}
}
