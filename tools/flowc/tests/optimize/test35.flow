import string;

// Constant struct fields.

Struct : (a : int, b : double, c : string, d : [int]);

main() {
	s = Struct(1, 2.0, "abc", [0, 1, 2]);
	println(s.a);
	println(s.b);
	println(s.c);
	println(s.d);
}
