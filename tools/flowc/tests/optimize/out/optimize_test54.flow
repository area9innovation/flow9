import runtime;
import string;
import tools/flowc/tests/optimize/aux_sources/for_test54;

// Test on dead code elimination.

unused_var = 1;
unused_func(x : int) { x - 1 }

main() {
	println(i2s(used_exfunc(1)));
	println(i2s(used_exvar));
}

----------

main = \ ->
	{
		println(i2s(used_exfunc(1)));
		println(i2s(used_exvar))
	}


