import runtime;

Equal(a: ?, b: ?, msg: string) -> bool {
    if (a == b) {
        println(msg + " OK");
        true
    } else {
        println("Failed: " + msg);
        false;
    }
}

main() {
    Equal(enumFromTo(0,0), [0], "enumFromTo(0,0)");
    Equal(enumFromTo(0,1), [0,1], "enumFromTo(0,1)");
    Equal(enumFromTo(1,0), [], "enumFromTo(1,0)");
    Equal(enumFromTo(2,0), [], "enumFromTo(2,0)");
    Equal(enumFromTo(-1,1), [-1,0,1], "enumFromTo(-1,1)");
}