Maybe<?> ::= None, Some<?>;
None();
Some(value : ?);


foo(a) {
	a.value;
}

goo(c) {
	if (c == None()) {
		0
	} else {
		// TODO: We could in principle specialize c in this branch, knowing it can not be None
		foo(c);
	}
}
