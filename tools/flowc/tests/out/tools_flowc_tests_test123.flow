import tuples;

afn(first : ?) -> int {
	pair : Pair<?, int> = Pair(first, 0);
	pair.second;
}

----------

afn = (\first:? ->
	(pair : Pair<?, int> = (Pair((first : ?), (0 : int)) : Pair<?, int>);
	({
		((pair : Pair<?, int>).second : int)
	}
	 : int) : int)
 : (first : ?) -> int);
