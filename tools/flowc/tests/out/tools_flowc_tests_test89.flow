	Maybe<?> ::= None, Some<?>;
	None();
	Some(value : ?);

FcSerializedModule();

preloadIncrementalModule() -> Maybe<FcSerializedModule>{
	sm = FcSerializedModule();
	if (true) {
		if (true) {
			Some(sm)
		} else {
			None()
		}
	} else None()
}


----------

preloadIncrementalModule = (\ ->
	(sm : FcSerializedModule = (FcSerializedModule() : FcSerializedModule);
	({
		(if ((true : bool)) {
			(if ((true : bool)) {
				(Some((sm : FcSerializedModule)) : Some<FcSerializedModule>)
			} else {
				(None() : None<FcSerializedModule>)
			}
			 : Maybe<FcSerializedModule>)
		} else {
			(None() : None<FcSerializedModule>)
		}
		 : Maybe<FcSerializedModule>)
	}
	 : Maybe<FcSerializedModule>) : Maybe<FcSerializedModule>)
 : () -> Maybe<FcSerializedModule>);
