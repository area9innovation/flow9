Maybe<?> ::= Some<?>, None;
	Some : (value : ?);
	None : ();

id(x : ?) -> ? {
	x
}

collision() -> void {
	a : Maybe<string> = id(None());
	b : Maybe<bool> = id(None());
}

----------

id = (\x:? ->
	(x : ?)
 : (x : ?) -> ?);

collision = (\ ->
	(a : Maybe<string> = ((id : (x : None<string>) -> None<string>)((None() : None<string>)) : None<string>);
	({
		(b : Maybe<bool> = ((id : (x : None<bool>) -> None<bool>)((None() : None<bool>)) : None<bool>);
		({

		}
		 : void) : void)
	}
	 : void) : void)
 : () -> void);
