None();
Some(a : ?);
Maybe<?> ::= None, Some<?>;

foo(a) {
	switch (a : Maybe) {
		None(): a;
		default: a;
	}
}

----------

foo = (\a:Maybe<?> ->
	(switch (a : Maybe<?>) {
		None(): {
			(a : None<?>)
		}
		default(): {
			(a : Maybe<?>)
		}
	} : Maybe<?>)
 : (a : Maybe<?>) -> Maybe<?>);
