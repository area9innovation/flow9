
FailStateResult<?> ::= Pair<?>, Failed;
    Pair(first : ?);
    Failed();

FailState(transform : (state : ?)->FailStateResult<?>);

makeFailState(s : FailState<?>, state) {
    r = s.transform(state);
/*    switch (r) {
        Pair(state1): r;
        Failed(): r;
    };*/
}

----------

makeFailState = (\s:FailState<?>, state:? ->
	(r : FailStateResult<?> = (((s : FailState<?>).transform : (?) -> FailStateResult<?>)((state : ?)) : FailStateResult<?>);
	({

	}
	 : void) : void)
 : (s : FailState<?>, state : ?) -> void);
