Maybe ::= None, Some;
	None();
	Some(value : int);

idfn(v : ?) -> ? {
	v
}

bar(m : ?) {
}

foo(x) {
	a = idfn(x);
	bar(a);
	bar(Some(0));
	baz(x);
}

baz(x : None) {

}

----------

bar = (\m:? ->
	({} : void)
 : (m : ?) -> void);

baz = (\x:None ->
	({} : void)
 : (x : None) -> void);

idfn = (\v:? ->
	(v : ?)
 : (v : ?) -> ?);

foo = (\x:None ->
	(a : None = ((idfn : (v : None) -> None)((x : None)) : None);
	({
		((bar : (m : None) -> void)((a : None)) : void);
		((bar : (m : Some) -> void)((Some((0 : int)) : Some)) : void);
		((baz : (x : None) -> void)((x : None)) : void)
	}
	 : void) : void)
 : (x : None) -> void);
