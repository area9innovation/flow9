Maybe<?> ::= None, Some<?>;
None();
Some(value : ?);

foo(b) {
	// debugtyping;
	baz = \a -> {};
	baz(None());
	b.value;
	baz(b);
}

----------

foo = (\b:Some<?> ->
	(baz : (Maybe<?>) -> void = (\a:Maybe<?> ->
		({} : void)
	 : (a : Maybe<?>) -> void);
	({
		((baz : (Maybe<?>) -> void)((None() : None<?>)) : void);
		((b : Some<?>).value : ?);
		((baz : (Maybe<?>) -> void)((b : Some<?>)) : void)
	}
	 : void) : void)
 : (b : Some<?>) -> void);
