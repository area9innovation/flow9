JsonObject(members : [Pair<string, JsonObject>]);
Pair(first : ?, second : ??);
Triple(first : ?, second : ??, third : ???);

// a = debugtyping1;

isJsonEqual(second : JsonObject) -> bool {
	objs = second.members;
	objs[0].first;
	objs[0].second;
	false
}

----------

isJsonEqual = (\second:JsonObject ->
	(objs : [Pair<string, JsonObject>] = ((second : JsonObject).members : [Pair<string, JsonObject>]);
	({
		(((objs : [Pair<string, JsonObject>])[(0 : int)] : Pair<string, JsonObject>).first : string);
		(((objs : [Pair<string, JsonObject>])[(0 : int)] : Pair<string, JsonObject>).second : JsonObject);
		(false : bool)
	}
	 : bool) : bool)
 : (second : JsonObject) -> bool);
