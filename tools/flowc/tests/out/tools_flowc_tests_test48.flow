FcInfo2(start : int, end : int);
FcInfo(name : string, rstart : int, end : int);


U ::= Foo, Bar;
Foo();
Bar();

foo(a) {
	switch (a) {
		Foo(): Foo();
		Bar(): Bar();
	}
}


----------

foo = (\a:U ->
	(switch (a : U) {
		Foo(): {
			(Foo() : Foo)
		}
		Bar(): {
			(Bar() : Bar)
		}
	} : U)
 : (a : U) -> U);
