Maybe<?> ::= None, Some<?>, Foo<?>;
	None();
	Some(value : ?);
	Foo(foo : ?);

foo(s) {
	a =
		if (true) {
			None();
		} else {
			if (true) {
				s.value
			} else {
				Foo(1);
			}
		}
}

----------

foo = (\s:Some<Foo<int>> ->
	(a : Maybe<int> = (if ((true : bool)) {
		(None() : None<int>)
	} else {
		(if ((true : bool)) {
			((s : Some<Foo<int>>).value : Foo<int>)
		} else {
			(Foo((1 : int)) : Foo<int>)
		}
		 : Foo<int>)
	}
	 : Maybe<int>);
	({

	}
	 : void) : void)
 : (s : Some<Foo<int>>) -> void);
