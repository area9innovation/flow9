Maybe<?> ::= None, Some<?>;
None();
Some(value : ?);

a : ref Maybe = ref None();

foo() {
//	debugtyping;
	a := Some(1);
	b = ^a;
}

----------

a = (ref (None() : None<?>) : ref None<?>);

foo = (\ ->
	({
		(((a : ref Maybe<int>)  :=  (Some((1 : int)) : Some<int>)) : void);
		(b : Maybe<?> = (^(a : ref Maybe<?>) : Maybe<?>);
		({

		}
		 : void) : void)
	}
	 : void)
 : () -> void);
