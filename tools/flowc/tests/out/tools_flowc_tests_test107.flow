foo() {
	compareTestValues = \f1 : flow, f2 : flow -> {
		false;
	}

	compareTestValues([0], [0]);
	compareTestValues(0, 0);

}

----------

foo = (\ ->
	(compareTestValues : (flow, flow) -> bool = (\f1:flow, f2:flow ->
		(false : bool)
	 : (f1 : flow, f2 : flow) -> bool);
	({
		((compareTestValues : (flow, flow) -> bool)(([(0 : int)] : [int]), ([(0 : int)] : [int])) : bool);
		((compareTestValues : (flow, flow) -> bool)((0 : int), (0 : int)) : bool)
	}
	 : bool) : bool)
 : () -> bool);
