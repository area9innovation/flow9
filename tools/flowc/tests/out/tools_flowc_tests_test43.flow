Pair(first : int, second : int);
Triple(first : int, second : int, third : int);

foo() {
	bar().second;
}

bar() {
	Pair(1, 2);
}

----------

bar = (\ ->
	(Pair((1 : int), (2 : int)) : Pair)
 : () -> Pair);

foo = (\ ->
	(((bar : () -> Pair)() : Pair).second : int)
 : () -> int);
