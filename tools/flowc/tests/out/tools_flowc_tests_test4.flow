native map : ([?], (?) -> ??) -> [??] = Native.map;

shuffleArray(array) {
//	debugtyping;
	nr = map(array, \no -> ref no);
}

----------

shuffleArray = (\array:[?] ->
	(nr : [ref ?] = ((map : ([?], (?) -> ref ?) -> [ref ?])((array : [?]), (\no:? ->
		(ref (no : ?) : ref ?)
	 : (no : ?) -> ref ?)) : [ref ?]);
	({

	}
	 : void) : void)
 : (array : [?]) -> void);
