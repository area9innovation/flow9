Acc(textStyle : [WigiTextStyle]);

WigiBlock(style : [WigiBlockStyle]);

WigiBlockStyle ::= WigiTextStyle, WigiName;

WigiTextStyle ::= Font, Color;

WigiName(label : string);

Font();
Color();

arrayPush(a : [?], e :?) -> [?] {
	a
}

foo(acc : Acc) {
	style = arrayPush(acc.textStyle, WigiName(""));
	e = WigiBlock(style);
}


----------

arrayPush = (\a:[?], e:? ->
	(a : [?])
 : (a : [?], e : ?) -> [?]);

foo = (\acc:Acc ->
	(style : [WigiBlockStyle] = ((arrayPush : (a : [WigiBlockStyle], e : WigiBlockStyle) -> [WigiBlockStyle])(((acc : Acc).textStyle : [WigiBlockStyle]), (WigiName(("" : string)) : WigiName)) : [WigiBlockStyle]);
	({
		(e : WigiBlock = (WigiBlock((style : [WigiBlockStyle])) : WigiBlock);
		({

		}
		 : void) : void)
	}
	 : void) : void)
 : (acc : Acc) -> void);
