export {
	pinchCapture : ref Maybe<int> = ref None();	
	Maybe<?> ::= Some<?>, None;
	Some(v : ?);
	None();
}

foo() {
	pinchCapture := Some(1);
}

----------

pinchCapture = (ref (None() : None<int>) : ref None<int>);

foo = (\ ->
	(((pinchCapture : ref Maybe<int>)  :=  (Some((1 : int)) : Some<int>)) : void)
 : () -> void);
