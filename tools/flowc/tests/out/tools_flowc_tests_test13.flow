import math/bits;

// foo=debugtyping;

bitRol(a, b) {
	bitOr(bitShl(a, b), bitUshr(a, 32 - b));
}

md5cmn(q, a, b, x, s, t){
	bitRol(a + q + x + t, s) + b;
}

// fooz = enddebugtyping;

----------

bitRol = (\a:int, b:int ->
	((bitOr : (int, int) -> int)(((bitShl : (a : int, n : int) -> int)((a : int), (b : int)) : int), ((bitUshr : (a : int, n : int) -> int)((a : int), (((32 : int) - (b : int)) : int)) : int)) : int)
 : (a : int, b : int) -> int);

md5cmn = (\q:int, a:int, b:int, x:int, s:int, t:int ->
	((((bitRol : (a : int, b : int) -> int)((((((((a : int) + (q : int)) : int) + (x : int)) : int) + (t : int)) : int), (s : int)) : int) + (b : int)) : int)
 : (q : int, a : int, b : int, x : int, s : int, t : int) -> int);
