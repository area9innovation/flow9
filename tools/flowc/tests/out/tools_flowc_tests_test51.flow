Tropic ::= TEmpty, TBorder;

TEmpty();
TBorder(left : double, top : double, right : double, bottom : double, form : Tropic);

TFixed(width : double, height : double) -> Tropic {
	if (width == 0.0 && height == 0.0) TEmpty()
	else TBorder(width, height, 0.0, 0.0, TEmpty())
}

Material ::= MText, MCenterX, MCols, Tropic;

MText(text : string, style : [int]);
MCols(m : [Material]);
MCenterX(m : Material);

// a = debugtyping;

foo() {
	MCols([TFixed(8.0, 0.0), MCenterX(MText("text", [])), TFixed(8.0, 0.0)]);
}

----------

TFixed = (\width:double, height:double ->
	(if ((((((width : double) == (0.0 : double)) : bool) && (((height : double) == (0.0 : double)) : bool)) : bool)) {
		(TEmpty() : TEmpty)
	} else {
		(TBorder((width : double), (height : double), (0.0 : double), (0.0 : double), (TEmpty() : TEmpty)) : TBorder)
	}
	 : Tropic)
 : (width : double, height : double) -> Tropic);

foo = (\ ->
	(MCols(([((TFixed : (width : double, height : double) -> Tropic)((8.0 : double), (0.0 : double)) : Tropic), (MCenterX((MText(("text" : string), ([] : [int])) : MText)) : MCenterX), ((TFixed : (width : double, height : double) -> Tropic)((8.0 : double), (0.0 : double)) : Tropic)] : [Material])) : MCols)
 : () -> MCols);
