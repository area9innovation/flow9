Some(value : ?);

// foo = debugtyping;
bar() {
	a : Some<int> = Some(0);
	a.value;
//	c : Some<int, double> = Some(2.0);
}

----------

bar = (\ ->
	(a : Some<int> = (Some((0 : int)) : Some<int>);
	({
		((a : Some<int>).value : int)
	}
	 : int) : int)
 : () -> int);
