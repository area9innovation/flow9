import ds/tuples;

export {

// We create a store which contains an array (myArray)
native syncedStore : (myArray: [?]) -> native = SyncedStoreNativeHost.syncedStore;
native webrtcProvider : () -> native = SyncedStoreNativeHost.webrtcProvider;
native disconnect : () -> void = SyncedStoreNativeHost.disconnect;
native connect : () -> void = SyncedStoreNativeHost.connect;

}
