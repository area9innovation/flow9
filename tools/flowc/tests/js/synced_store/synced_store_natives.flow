import ds/tuples;

export {

// We create a store which contains an array (myArray)
native syncedStore : (myArray: [?]) -> native = SyncedStoreNativeHost.store;
native webrtcProvider : () -> native = SyncedStoreNativeHost.webrtcProvider;
native syncedStoreDisconnect : () -> void = SyncedStoreNativeHost.disconnect;
native syncedStoreConnect : () -> void = SyncedStoreNativeHost.connect;

}
