import qexp;
import runtime;

main() {
    a = 1;
	b = [1];

	@1 + $([1]);
	@$b;
	@$(1+a);
	@{1;2};
	@if (1) 2 else 3;
	@{a = 1; a};
	@foo(1, $a);
 	@1;
    @$a + 1;
    @1.0 * 2 / true - "a";
	@a;
	@\a->0;
	@1+$(2.0);

    {}
}
