import test144_2;

export {
	Good : (table : Mb<?>);	// No error
	Bad : (table : SqlCreateTable3<?>);	// Struct Bad: Expected 1 type parameters to SqlCreateTable3, but got 1
} 

f(table : Sm<?>) -> Good {
	Good(table);
}

buildSqlTableInfo(table : SqlCreateTable3<?>) -> Bad {
	Bad(table) ;
}
