import interactive;

native prints : (string) -> void = host.prints;
native makeClip_a : io () -> native = RenderSupport.makeClip;

main() {
	prints("start");

	clip = makeClip_a();

	x = getMouseX(clip);
	y = getMouseY(clip);

	prints("finish");
}
