import fusion;

native prints : (string) -> void = host.prints;
native printi : (int) -> void = host.printi;

main() {
	prints("start");

	b = make(1);

	uns = makeSubscribe(b, \f -> {
		printi(f);
	});

	next(b, 2);
	uns();

	prints("finish");
}
