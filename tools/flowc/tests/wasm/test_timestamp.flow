native prints : (string) -> void = host.prints;
native timestamp : () -> double = host.timestamp;

main () {
	d1 = timestamp();

	prints(cast(d1 : double -> string));

	d2 = timestamp();

	prints(cast(d2 : double -> string));
}
