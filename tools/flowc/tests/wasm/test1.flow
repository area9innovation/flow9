import expect;

export {
	add(i1 : int, i2 : int) -> int;
	sub(i1 : int, i2 : int) -> int;
}

add(i1 : int, i2 : int) -> int {
	i1 + i2;
}

sub(i1 : int, i2 : int) -> int {
	i1 - i2;
}

main() {
	a = 1;
	expect(0, add(a,2) == 3, true);
	expect(1, sub(a,2) == -1, true);
}