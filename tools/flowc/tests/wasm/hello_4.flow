import fullscreen;

native prints : (string) -> void = host.prints;

main() {
	prints("start");

	d = onFullScreen(\isFS -> {
		prints("onFullScreen: " + (if(isFS) "true" else "false"));
	});

	toggleFullScreen(true);
	toggleFullScreen(false);

	prints("finish");
}
