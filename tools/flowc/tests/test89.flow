	Maybe<?> ::= None, Some<?>;
	None();
	Some(value : ?);

FcSerializedModule();

preloadIncrementalModule() -> Maybe<FcSerializedModule>{
	sm = FcSerializedModule();
	if (true) {
		if (true) {
			Some(sm)
		} else {
			None()
		}
	} else None()
}

