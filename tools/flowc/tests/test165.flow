import behaviour;

MyTransform<?> ::= Behaviour<?>, MyBehaviour<?>;

MyBehaviour(arg : ?);
DummyTemplate(arg : ?);

testFn(a : DynamicBehaviour<int>) -> void {
	next(a, 3);
};

main() {
	b1 = DummyTemplate(const(1));
	testFn(b1.arg);
}