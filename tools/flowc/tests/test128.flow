import runtime;
import string;
import tools/flowc/tests/errors/aux_sources/for_error21;

// Test on unused exports/functions detection.

unused_var = 1;
unused_func(x : int) { x - 1 }

main() {
	println(i2s(used_exfunc(1)));
	println(i2s(used_exvar));
}
