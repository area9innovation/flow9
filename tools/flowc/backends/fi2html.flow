import tools/flowc/backends/common;
import tools/flowc/incremental/fiprogram;

export {
	fi2html(program : FiProgram, cfg : FcHTMLConfig, callback : () -> void) -> void;
}

fi2html(program, cfg, callback) {
	if (cfg.verbose > 0)
		fcPrintln("TODO: Making HTML '" + cfg.outputfile + "'...");
	callback();
}
