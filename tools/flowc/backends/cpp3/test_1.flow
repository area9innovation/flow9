import runtime;
import string;

A = 12;

b = "A" + "27";

add(x : int, y : int) -> int {
	x + y;
}

fact(x : int) -> int {
	if (x == 0) 1 else x * fact(x - 1);
}

main() -> void {
	println("5! = " + i2s(fact(5)));
	println("A = " + i2s(A));
	println("b = " + b);
	quit(0);
}