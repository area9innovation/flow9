[

Cpp3NativePlain("genericCompare", true, 
"template<typename T> inline Int $F_1(genericCompare)(T x, T y);\n",
"template<typename T>
inline Int $F_1(genericCompare)(T x, T y) {
	Int ret = static_cast<Int>(compare(x, y));
	decRc<T>(x);
	decRc<T>(y);
	return ret;
}\n", "", [], []
)
]