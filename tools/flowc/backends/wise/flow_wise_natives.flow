import ds/tree;

export {
	// flow_name -> wise_name
	findNativeName(name : string) -> Maybe<string>;
}

findNativeName(name : string) -> Maybe<string> {
	lookupTree(registeredNatives, name);
}

registeredNatives = makeTree1("trunc", "trunc_s<>");