import ds/tree;

export {
	// flow_name -> wise_name-arg_number
	findNativeName(name : string) -> Maybe<Pair<string, int>>;
}

findNativeName(name : string) -> Maybe<Pair<string, int>> {
	lookupTree(registeredNatives, name);
}

registeredNatives = makeTree1("trunc", Pair("trunc_s<>", 1));