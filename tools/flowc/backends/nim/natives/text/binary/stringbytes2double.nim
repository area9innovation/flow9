# native stringbytes2double : (str : string) -> double = Native.stringbytes2double;
# Read 8 bytes of the string in UTF-16 and converts to a double

proc stringbytes2double(str : string) : float =
    0.0