import tools/flowc/backends/cpp2/fi2cpp_compile;

export {
	fi2cpp2(program : FiProgram, cfg : FcCppConfig, callback : () -> void) -> void;
}

fi2cpp2(program : FiProgram, cfg : FcCppConfig, callback : () -> void) -> void {
	//fcVerbose(cfg, 0, "Making cpp '" + cfg.flowfile + "'...");

	callback();
}
