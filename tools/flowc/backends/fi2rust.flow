import tools/flowc/backends/common;
import tools/flowc/incremental/fiprogram;

export {
	fi2rust(program : FiProgram, cfg : FcRustConfig, callback : () -> void) -> void;
}

fi2rust(program, cfg, callback) {
	if (cfg.verbose > 0)
		fcPrintln("TODO: Making Rust '" + cfg.outputfile + "'...");
	callback();
}
