import tools/flowc/typechecker/ftype;

export {
	Unify(flowfile : [string], bindings : [FBinding], constraints : [FConstraint], final : [bool]);

		FBinding(tyvar : FTypeVar, type : FType);
		FConstraint(from : FType, kind : FUnification, to : FType);
}
