import runtime;

fact(i : int) -> int {
	if (i <= 1) i
	else i * fact(i - 1)
}

prec() -> int {
	1 + 2 * 3 + 4 % 6 + 2 * (3 + 4);
}



main() {
	prec();
	println(fact(0xffffffff));
	println("Hello world");
}
