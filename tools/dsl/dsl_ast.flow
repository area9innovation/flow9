import ds/list;

export {
	DslAst ::= DslBool, DslInt, DslDouble, DslString, DslList, DslNode;
		DslBool(value : bool);
		DslInt(value : int);
		DslDouble(value : double);
		DslString(value : string);
		DslList(value : List<DslAst>);
		DslNode(name : string, args : [DslAst], pos : int);
}
