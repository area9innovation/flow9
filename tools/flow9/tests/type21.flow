import behaviour;

dynArrayPush : (DynamicBehaviour<[?]>, ?) -> void;

dynArrayPush(dynamicArray /*: DynamicBehaviour<[?]> */, elem /*: ?*/) -> void { 
	next(dynamicArray, arrayPush(getValue(dynamicArray), elem))
}
