native println : io (?) -> void = Native.println;

Maybe ::= None, Some;
None();
Some();

main() {

	n : Maybe = if (1 > 0) Some() else None();
	/*println(switch (n) {
		Some(): "some";
		None(): "none";
	}); // some*/
}