native println : io (?) -> void = Native.println;

//  error: could not convert ‘"name"’ from ‘const char [5]’ to ‘std::u16string’
Some(name : string);

main() {
	m = Some("name");
	println(m.name);
}
