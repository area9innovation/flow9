uniqq(a : [?]) -> [?] {
	println("in uniqq");
	a
}

native println : io (?) -> void = Native.println;
native quit : io (code : int) -> void = Native.quit;

main() -> void {
	// println("Test3.1 ...");
	// res1 = uniqq([2, 3, 4]);
	arr = [1];
	uniqq(arr);
	println("END");
	//quit(0);
}