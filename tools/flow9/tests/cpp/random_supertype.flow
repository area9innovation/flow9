native println : io (?) -> void = Native.println;

None();
Some();

AnyStruct ::= Some, Any, None;
Any();

Maybe ::= None, Some;

main() {

	// TODO
	// doesn't work : AnyStruct in cpp, but should be Maybe
	i = 5;
	v1 = if (i > 5) Some() else if (i > 4) Any() else None(); //error : Picking random supertype from
	switch (v1) {
		Some(): println("some");
		None(): println("none");
		AnyStruct() : println("any");
	} // any

	// doesn't work : AnyStruct in cpp, but should be Maybe
	v2 : Maybe = if (1 > 0) Some() else None(); //error : Picking random supertype from
	switch (v2 : Maybe) {
		Some(): println("some");
		None(): println("none");
	} // some
}
