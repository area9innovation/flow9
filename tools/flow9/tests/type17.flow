import ds/tree;

Behaviour<?> ::= ConstBehaviour<?>, DynamicBehaviour<?>;
	DynamicBehaviour(value : ref ?);
	ConstBehaviour : (constv : ?);

nextCounterTree : DynamicBehaviour<Tree<string, int>> = DynamicBehaviour(ref makeTree());
