native println : io (flow) -> void = Native.println;

foo() {
	a = "1";
	println(a);
	b = a + "2";
}

main() {
	foo();
}

/*

foo() {
	a = dup( dup("1") ); // Since a is referenced two times, we add a dup
	print(a);
	b = +(a, "2");
	a;
}

main() {
	drop(foo());
}

+(a : string, b : string) -> string {
	dup(native_+(drop(a), drop(b));
}

*/
