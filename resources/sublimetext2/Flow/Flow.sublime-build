{
    "working_dir": "${project_path:${folder}}",
	"cmd": ["compile.bat", "$file", "$project_path"],
	"file_regex": "(?<file>.*\\.flow):(?<line>[0-9]*):",
	"linux":
    {
        "cmd": ["compile.sh", "$file", "$project_path"]
    }
}
