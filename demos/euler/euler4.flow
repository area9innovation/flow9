import runtime;
import math/math;

/*
A palindromic number reads the same both ways. The largest palindrome made 
from the product of two 2-digit numbers is 9009 = 91 × 99.

Find the largest palindrome made from the product of two 3-digit numbers.
*/

isPalindromic(s : string) -> bool {
	l = strlen(s);
	if (l <= 1) true
	else {
		first = strLeft(s, 1);
		last = strRight(s, l - 1);
		if (first == last) {
			isPalindromic(strsubsmart(s, 1, -1));
		} else false;
	}
}

euler4(a : int, b : int, biggest: int) -> int {
	p = a * b;
	nbiggest = if (p < biggest) biggest else if (isPalindromic(i2s(p))) p else biggest;
	if (b > 100) {
		euler4(a, b - 1, nbiggest)
	} else {
		if (a > 100) {
			euler4(a - 1, 999, nbiggest)
		} else {
			nbiggest;
		}
	}
}

main() {
	println(euler4(999, 999, 0));
	quit(0);
}
