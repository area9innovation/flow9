import fabric/fabric;

main() -> void {
	ui = BTableX(generate(0, 100, \y -> {
		generate(0, 26, \x -> {
			if (y == 0) {
				BText(fromCharCode(65 + x), [])
			} else {
				BTextInput("", [MWidth(100.0)], [])
			}
		})
	}), BTableStyle());
	brender(ui);
	{}
}
