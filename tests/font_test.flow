import material/tests/material_test_utils;

main() {
	MText("SAMPLE", [MButton1(), MWhite()])
	|> (\f -> MFrame(0.0, 0.0, [MFill(MBlue(500))], f))
	|> (\f -> MScale(const(Factor(20.0, 20.0)), f))
	|> debugRender
}