import material/material_ui;

main() {
	setRendererType("html");
	mrender(makeMaterialManager([]), true, MFrame(16.0, 0.0, [Fill(0xf2f2f2)], 
		MAvailableWidth(
			MParagraph("Here are some tips on how to customize the speech:", [MListBody()])
			|> MDebugMetricsGreen,
			const(324.0)
		)
	));
}