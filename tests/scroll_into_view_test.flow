import material/material_ui;

main() {
	manager = makeMaterialManager([]);

	m =
		// MScroll(
		// 	MLines2(
		// 		TFixed(10000.0, 10000.0),
				MScroll(
					MLines2(
						TFixed(10000.0, 10000.0),
						MScroll(
							MTextInput(make("test"), [], []),
							TFixed(10000.0, 10000.0),
							[]
						);
					),
					TFixed(10000.0, 10000.0),
					[]
				)
		// 	),
		// 	TFixed(10000.0, 10000.0),
		// 	[]
		// );

	mrender(manager, true, m);
}