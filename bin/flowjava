#!/bin/bash

set -e

if [ `uname` == Darwin ]; then
    READLINK=greadlink
else
    READLINK=readlink
fi

SCRIPT_FN=`$READLINK -e "$0"`
SCRIPT_DIR=`dirname "$SCRIPT_FN"`

BASE_DIR=`$READLINK -e "$SCRIPT_DIR/.."`

# Compile the runtime
pushd $BASE_DIR/src/java
javac -g com/area9innovation/flow/*.java com/area9innovation/flow/javafx/*.java
popd

# Generate the Java for our program
flow --java javagen $@

# Compile the generated code
javac -Xlint:unchecked -encoding UTF-8 -cp $BASE_DIR/src/java javagen/*.java

# Run the program!
java -cp $BASE_DIR/src/java:. com.area9innovation.flow.javafx.FxLoader
