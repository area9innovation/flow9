import runtime;

input : [int] = [1,2,3,4,5];

transform(input : [int]) -> [string] {
    map(input, i2s);
}
main() {
    println(input);
    output : [string] = transform(input);
    println(output);
}