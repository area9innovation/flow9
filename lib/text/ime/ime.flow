export {
	InputMethodAPI(
		getPredictions : (context : string, current : string, packId : Maybe<?>, count : int) -> InputMethodPredictionChoices
	);

	InputMethodPredictionChoices(
		previousId : ?;
		nextId : ?;
		choices : [InputMethodPredictionChoice];
	);

	InputMethodPredictionChoice(
		text : string;
		hotkey : string;
	);

	getTestPinyin() -> InputMethodAPI;
}

getTestPinyin() -> InputMethodAPI<int> {
	InputAPI(
		\context : string, current : string, packId : Maybe<int>, count : int -> InputMethodPredictionChoices(
			// TODO
			0, 0,
			[]
		)
	)
}
