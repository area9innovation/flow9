import text/compression/lzw;
import text/compression/lzw_bitpack;

export {
	decompress(data : string) -> string;
}

decompress(data : string) -> string {
	if (startsWith(data, "LZW:")) decompressLzw(data)
	else if (startsWith(data, "LZWBP:")) decompressLzwBp(data)
	else data
}