// Automatically generated from Mango grammar
export {
	Sexpr ::= SSBool, SSConstructor, SSDouble, SSInt, SSList, SSOperator, SSSpecialForm, SSString, SSVariable, SSVector;

	Special_name ::= SAnd, SBegin, SClosure, SDefine, SEval, SIf, SImport, SLambda, SLet, SLetRec, SList, SMatch, SOr, SQuasiQuote, SQuote, SSet, SUnQuote, SUnQuoteSplicing;

	SAnd();
	SBegin();
	SClosure();
	SDefine();
	SEval();
	SIf();
	SImport();
	SLambda();
	SLet();
	SLetRec();
	SList();
	SMatch();
	SOr();
	SQuasiQuote();
	SQuote();
	SSBool(bool1 : bool);
	SSConstructor(constructor_atom : string);
	SSDouble(double1 : double);
	SSInt(int1 : int);
	SSList(sexprs : [Sexpr]);
	SSOperator(operators : string);
	SSSpecialForm(special_name : Special_name, sexprs : [Sexpr]);
	SSString(string1 : string);
	SSVariable(variable_atom : string);
	SSet();
	SUnQuote();
	SUnQuoteSplicing();
	SSVector(sexprs : [Sexpr]);
}
