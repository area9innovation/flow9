// Automatically generated from Mango grammar
export {
	Sexpr ::= SSBool, SSConstructor, SSDouble, SSInt, SSList, SSOperator, SSQuasiQuote, SSQuote, SSString, SSUnquote, SSUnquoteSplicing, SSVariable;

	SSBool(bool1 : bool);
	SSConstructor(constructor_atom : string);
	SSDouble(double1 : double);
	SSInt(int1 : int);
	SSList(sexprs : [Sexpr]);
	SSOperator(string1 : string);
	SSQuasiQuote(sexpr : Sexpr);
	SSQuote(sexpr : Sexpr);
	SSString(string1 : string);
	SSUnquote(sexpr : Sexpr);
	SSUnquoteSplicing(sexpr : Sexpr);
	SSVariable(variable_atom : string);
}
