import lib/rewrite;

let program = '(d + (b * c) + a);

let rules = '[
	Rule("Canonical order", `+`(a, b, ...) => `+`(b, a, ...) if a > b),
];

let withTypes = applyRulesUntilFixedPoint(program, rules, 5);
println(withTypes[0])
