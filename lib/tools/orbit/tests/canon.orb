import lib/rewrite;

let program = '(b + a + 2 + 1);

let rules = '[
	Rule("Canonical order", `+`(...) => `+`(sort(...))),
];

let withTypes = applyRulesUntilFixedPoint(program, rules, 5);
println(withTypes[0])
