import lib/rewrite;

let program = '(d + (b * c) + a);

let rules = '[
	Rule("Canonical order", `+`(a, ...) => `+`(sortUnique(...))),
];

let withTypes = applyRulesUntilFixedPoint(program, rules, 5);
println(withTypes[0])
