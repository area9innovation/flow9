// Test file for RunCore integration with Orbit

// Basic arithmetic functions test
let a = 5;
let b = 7;
let sum = a + b;

// Test string functions
let s1 = "Hello";
let s2 = "World";
let concat = s1 + " " + s2;

// Test array operations
let arr = [1, 2, 3, 4, 5];
let length_arr = length(arr);

// Test logical operations
let c = a > b;
let d = a < b;
let e = a = 5;
let f = c && d;
let g = c || d;
let h = !c;

// Test string functions
let s = "example string";
let len = strlen(s);
let sub = substring(s, 0, 7);

// Test conversions
let num_str = i2s(42);
let str_num = s2i("123");

// Test nested function calls
let nested = i2s(s2i("42") + 8);

// Print results
println("Sum: " + i2s(sum));
println("Concatenated: " + concat);
println("Array length: " + i2s(length_arr));
println("Comparison c (a > b): " + i2s(c));
println("Comparison d (a < b): " + i2s(d));
println("Comparison e (a == 5): " + i2s(e));
println("Logical AND (c && d): " + i2s(f));
println("Logical OR (c || d): " + i2s(g));
println("Logical NOT (!c): " + i2s(h));
println("String length: " + i2s(len));
println("Substring: " + sub);
println("Number to string: " + num_str);
println("String to number: " + i2s(str_num));
println("Nested calls: " + nested)