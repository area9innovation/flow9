// Simple test for match with condition

println("Testing conditional pattern matching");

// Set up the test value
let n = 5;

println("n = " + n);

// Try a simple match with one condition
n is (
  x => println("n is greater than zero") if x > 0;
  
  x => println("n is not greater than zero")
)