fn quote(a : ast) = a;
let g = makeOGraph("graph");
let f = quote(\x -> 2*x);
let n = addOGraphWithSub(g, f, []);
println(extractOGraph(g, n));
// setFileContent("test.dot", ograph2dot(g));

matchOGraphPattern(g, quote(2*a), \(b,e) -> (
	println("Pattern matched");
	println(extractOGraph(g, e));
));

// f is (\a.b => Lambda(a, b)); // Matches a lambda with one arg
