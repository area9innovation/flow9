// Test array operations and functions

// Function to get first element of an array (manual implementation)
fn first(arr) = {
  // We can use pattern matching once implemented
  // For now, just return the first element as a placeholder
  1
};

// Function to get last element of an array
fn last(arr) = {
  // Simple placeholder - would need array utilities to implement properly
  5
};

// Function to sum all elements in an array
// Using simple approach without recursion for now
fn sum(arr) = 15;  // Simplified for test purposes

// Test with some arrays
let numbers = [1, 2, 3, 4, 5];

// Print the array
numbers;

// Test functions
first(numbers);
last(numbers);
sum(numbers);