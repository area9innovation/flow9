// Extremely simplified version to debug the issues

// Define test values for variables
let x = 42;
let y = 100;

// Main demonstration
println("Creating ograph...");
let graph = makeOGraph("test_demo");

// Test with a simple expression
println("\nAdding x + 0 to graph");
let expr1 = OrAdd(OrInt(10), OrInt(0));

// Add the expression to the ograph
let nodeId = addNodeToOGraph(graph, expr1);
println("Added node with ID: " + i2s(nodeId));

// Add a domain annotation
let addedDomain = addDomainToNode(graph, nodeId, "Simplified");
println("Added domain 'Simplified': " + prettyOrbit(addedDomain));

// Print the graph
println("\nPrinting ograph:");
printOGraph(graph)