// More complex array tests

// Arrays with expressions
[1 + 2, 3 * 4, 5 / 2];

// Mixed type arrays (if supported)
[1, "hello", true];

// Array with function call result
fn add(a, b) = a + b;
[add(1, 2), add(3, 4)];

// Using arrays in expressions
let arr1 = [1, 2, 3];
let arr2 = [4, 5, 6];
arr1;
arr2;