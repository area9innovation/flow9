List<?> ::= EmptyList, Cons<?>;
	EmptyList();
	// Add head to the list "tail"
	Cons(head : ?, tail : List<?>);

makeList() -> List<?> {
	EmptyList()
}

pushPEnv(value : flow) -> void {
}

foo() {
	pushPEnv(makeList());
}
