flow(i : ?) -> flow {
	cast(i : ? -> flow);
}

toString(v : ?) -> string {
	toString2(flow(v));
}

native toString2 : (flow) -> string = Native.toString;

flow2b(f : flow) -> bool {
	cast(f : flow -> bool);
}
