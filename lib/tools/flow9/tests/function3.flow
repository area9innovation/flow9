applyTwice(func : (int) -> int, value : int) -> int {
    func(func(value))
}

increment(x : int) -> int {
    x + 1
}

testHigherOrderFunction() {
    result = applyTwice(increment, 3);
    result
}
