applyFunctionTwice(func : (int) -> int, x : int) -> int {
    func(func(x))
}

addThree(x : int) -> int {
    x + 3
}

testCombineFirstClassAndHigherOrder() {
    result = applyFunctionTwice(addThree, 4);
    result
}
