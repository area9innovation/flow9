import testinc/test7a;

export {
	// Function that calls the function from test7a
	displayProcessedValue(x: int) -> string;
}

// This function should be re-typechecked when processValue's parameter type changes
displayProcessedValue(x: int) -> string {
	"Display: " + processValue(x)
}