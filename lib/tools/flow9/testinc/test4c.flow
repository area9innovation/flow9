import testinc/test4b;
import testinc/test4a;

export {
	// Final function using both intermediate and base
	combineOperations(b: BaseType) -> int;
}

// This should be recompiled when either enhanceBase or BaseType changes
combineOperations(b: BaseType) -> int {
	processBase(b) + enhanceBase(b)
}