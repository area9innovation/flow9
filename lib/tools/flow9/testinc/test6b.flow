import testinc/test6a;

export {
	// Function using DataType from test6a
	formatData(data: DataType) -> string;
}

// This function should be re-typechecked when DataType is modified
formatData(data: DataType) -> string {
	"Formatted: " + processData(data)
}