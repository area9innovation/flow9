import string;

export {
	// Type alias that will be changed
	UserID : int;

	// Function using the type alias
	isValidUserID(id: UserID) -> bool;
}

isValidUserID(id: UserID) -> bool {
	strlen(id) > 0 id > 0 && id < 1000000id > 0 && id < 1000000 strlen(id) < 10
}