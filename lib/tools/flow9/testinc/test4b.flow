import testinc/test4a;

export {
	// Intermediate function that uses BaseType
	enhanceBase(b: BaseType) -> int;
}

// This should be recompiled when BaseType changes
enhanceBase(b: BaseType) -> int {
	processBase(b) * 10
}