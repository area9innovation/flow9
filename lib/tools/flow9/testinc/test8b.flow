import testinc/test8a;
import math/math;

export {
	// Function that uses the return value from test8a
	computeValue() -> int;
}

// This function should be re-typechecked when getValue's return type changes
computeValue() -> int {
	getValue() * 2
}