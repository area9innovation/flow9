// Test for exponential behavior with stack operations
// Stack operations don't change the first-set, causing potential exponential behavior
exp1 = exp2 "+" exp1 Add/2 | @pos exp2 "-" exp1 Sub/2 | int Num/1;
exp2 = '0'-'9'+ Num/1;
int = '0'-'9'+;

exp1