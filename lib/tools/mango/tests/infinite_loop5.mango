// Test case 5: Complex sequence that can match empty
// Both parts of the sequence can match empty: "a"? and "b"?
// So ("a"? "b"?)* can cause infinite loops
complexLoop = ("a"? "b"?)* ComplexLoop/1;

complexLoop