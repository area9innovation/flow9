// Test case: No infinite loop risk
// "a" can't match empty, so "a"* won't cause infinite loops
safeLoop = "a"* SafeLoop/1;

// Plus operator requires at least one match, so also safe
safePlus = ("a"?)+ SafePlus/1;

safeLoop