import material/material2tropic;

main() {
	content = MChip("CHIP", [MCloseButton(), MOnClick(\ -> println("click")), MCustomCloseButtonIcon("android")]);
	mrender(makeMaterialManager([]), true, content);
}