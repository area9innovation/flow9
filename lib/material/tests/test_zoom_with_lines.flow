import material/material;
import material/material_manager;
import material/material_gui;
import material/material2tropic;

main() {
	mManager = makeMaterialManager([]);

	rect = TRectangle([Fill(red)], TFixed(600.0, 600.0));
	rect2 = MZoom(rect, TFillXHT(make(300.)), true);

	content = MLines([
		rect2,
		MText("any text here", [])
	]);

	m = content;
	mrender(mManager, true, m);
}