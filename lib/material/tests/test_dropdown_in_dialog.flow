import material/material2tropic;
import material/material_dialog;

main () {
	manager = makeMaterialManager([]);

	MIconButton("add", \ -> {
		ShowMDialog(manager, make(false), [MDialogClickOutToClose(), MDialogUseFrame()], MIconButton("add", \ -> {
			ShowMDialog(manager, make(false), [MDialogClickOutToClose(), MDialogUseFrame()],
				MBaselineCols([
					MText("Title:", []),
					MFillX(),
					MDropDown(make(-1), _("AUTO"), map(enumFromTo(0, 100), \i -> "rfjkghjkhg dfgh dfjkghjfhdg dfkghkjdfh " + i2s(i)), [MAddClearButton()]),
				]));
		}, [], []))
	}, [], [])
	|> (\m -> mrender(manager, true, m));
}