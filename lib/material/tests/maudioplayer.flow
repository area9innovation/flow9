import material/material2tropic;
import material/material_ui;

main() {
	player = MAudioPlayer(
		"./test.mp3",
		// "lib/material/tests/test.mp3",
		[FVideoPlay(make(true))]
	);

	manager = makeMaterialManager([]);

	mrender(manager, true, player)
}
