import material/material2tropic;

main() {

	manager = makeMaterialManager([]);

	content =
		MIcon("android", [MIconSize(40.), MIconCounter(const(100), [MIconCounterLimited(), MIconSize(16.)])])
		|> MBorderTop(200.)
		|> MBorderLeft(200.);

	mrender(manager, true, content);
}