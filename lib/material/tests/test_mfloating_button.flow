import material/material_ui;

main() {
	manager = makeMaterialManager([]);
	setRendererType("html");

	content =
		MFloatingButton("android", [MFloatingButtonCenteredShape(), MFabSize(48.)], []);

	mrender(manager, true, content);
}