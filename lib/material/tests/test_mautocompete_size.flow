import material/tests/material_test_utils;

main() {
	m =
		// MEllipsisText("  ", [])
		MAutoComplete(make("smth"), [], [], [])
		// MTextInput(make(""), [], [])
		// |> (\m -> MDebugMetrics(green, m))
		|> (\m -> MLines2(m, MText("text", [])))
		;

	debugRender(m)
}