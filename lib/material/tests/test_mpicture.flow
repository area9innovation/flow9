import material/material2tropic;

main() {
	manager = makeMaterialManager([]);
	whB = makeWH();

	content = 
		MPicture(
			"http://palhansen.com/content/images/stories/pele-6533487.jpg",
			// "http://frugalfrolicker.com/wp-content/uploads/2015/07/Kangaroo-Valley-10.jpg",
			[
				MPictureCustomLoading(
					MText("LOADING", [])
				),
				// WidthHeight(-1., -1.),
				// WidthHeight(0., 0.),
				// WidthHeight(200., 200.),
				TPictureSize(fwhdivide(whB, const(WidthHeight(2., 2.))))
				// TPictureSize(whB),
				// MPictureSize(whB)
			]
		)
		|> (\t -> MAttachAvailable(t, whB));
	
	mrender(manager, true, content);
}