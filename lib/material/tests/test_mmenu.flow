import material/material2tropic;

main() {
	manager = makeMaterialManager([]);

	content =
		MMenu(
			MTextButton("BUTTON", nop, [], []), [
				MMenuSingleLine("smth", [
					MSecondaryAction(MTextInput(make(""), [], []))
				])
			], []
		);
	
	mrender(manager, true, content);
}