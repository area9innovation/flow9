import material/material2tropic;

main() {
	setRendererType("html");

	time = make(nullTime);
	
	content =
		MTimePicker(time, [
			// MTimeMin(Time(0, 0, 0, 2, 30, 00)),
			MTimeMin(Time(2023, 2, 2, 5, 0, 0)),
			// MTimeMin(Time(0, 0, 0, 5, 0, 0)),
			// MTimeMax(Time(0, 0, 0, 22, 00, 00)),
		]);

	mrender(makeMaterialManager([]), true, content);
}