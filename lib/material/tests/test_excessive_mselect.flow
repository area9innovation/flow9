import material/material2tropic;
import material/material_manager;
main() {
	mrender(
		makeMaterialManager([]),
		true,
		MDataTable(
			[MColumn("Test", "", 200, [])],
			[[MSelect(make(false), \__ -> { println("hi"); MText("test", [MDataRow()]) })]],
			[MFullWidth()]
		)
	) |> ignore
}