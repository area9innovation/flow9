import material/material2tropic;

main() {
	manager = makeMaterialManager([]);
	content = 
		MSlider(make(0.0), [
			// MPurple(900),
			MItemsColor(MPurple(900)),
			MWidth(400.0),
			MSliderIconLeft("android"),
			MSliderIconRight("android"),
			MSliderDisplayValue(true),
			MSliderRange(0., 50.),
			MSliderStep(10.)
		]);

	mrender(manager, true, content);
}