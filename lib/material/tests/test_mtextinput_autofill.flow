import material/material2tropic;

main() {
	setRendererType("html");

	content =
		MTextInput(
			make(""),
			[
				TextInputType(EmailType()),
				AutoCompleteType(ACUserName())
			],
			[
			
			]
		);

	mrender(makeMaterialManager([]), true, content);
}