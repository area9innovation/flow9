import material/material_ui;

main() {
	// manager = makeMaterialManager([]);
	setRendererType("html");
	
	content =
		TLines2(
			TRectangle([MStroke(MBlack())], TFixed(200., 200.)),
			TRectangle([MStroke(MBlack())], TFixed(200., 200.))
		)
		|> TBorderTop(200.)
		|> TBorderLeft(200.)
		// |> TCanvas
		;

	// mrender(manager, true, content);
	trender(content, [])
}