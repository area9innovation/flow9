import material/material2tropic;

main() {
	manager = makeMaterialManager([]);
	// xVisible = make(false);

	content =
		MScroll(
			TFixed(201., 200.)
			|> addTBackground(MGreen(200)),
			TFixed(200., 200.),
			[
				MScrollFadeContent(white, 10.),
				// MScrollbarsVisible(xVisible, make(false)),
				MScrollCropByContent()
			]
		);
	
	mrender(manager, true, content);
}