import material/material2tropic;

main() {
	manager = makeMaterialManager([]);

	dropDown = 
		MDropDown(make(-1), "select", ["iteeeeeeem 1", "item 2", "iteeeeeeeeeeeeeeem 3"], [
			MNonSpecifiedAlwaysOnTop()
		]);
		
	content =
		dropDown
		|> MCenter;

	mrender(manager, true, content);
}