import material/material_ui;

main() {
	manager = makeMaterialManager([]);

	m =
		MCols([
			TForm(
				maskImage(
					Picture("images/todo_collapse.png", []),
					black
				)
			),
			TMaskImage(
				TPicture("images/todo_expand.png", []),
				black
			),
			TMaskImage(
				TBorder4(20.0, TPicture("https://ccl.area9learning.com/flow/images/tagpicker/Grade.png", [])),
				red
			),
			MBorder4(20.0,
				MScale(
					const(Factor(1.5, 1.5)),
					TMaskImage(
						TBorder4(20.0, TPicture("https://ccl.area9learning.com/flow/images/tagpicker/Grade.png", [])),
						red
					)
				)
			)
		]);

	mrender(manager, true, m);
}

TMaskImage(content : Tropic, color : int) -> Tropic {
	wh = makeWH();

	TMask(
		TRectangle([Fill(black)], TSized(wh)),
		TInspect([ISize(wh)], content)
	)
}