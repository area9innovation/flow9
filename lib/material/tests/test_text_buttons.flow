import material/material_dialog;

main() {
    manager = makeMaterialManager([]);
    mrender(manager, true,
    MAppStructure([
        MContent(
            MCols([
                TFillX(),
                MTextButton("button1", nop, [MButtonRaised()], []),
                MTextButton("button2", nop, [MButtonRaised()], []),
                MTextButton("button3", nop, [MButtonRaised()], []),
                MTextButton("button4", nop, [MButtonRaised()], []),
                MTextButton("button5", nop, [MButtonRaised()], []),
                MTextButton("button6", nop, [MButtonRaised()], []),
                MTextButton("button7", nop, [MButtonRaised()], []),
                MTextButton("button8", nop, [MButtonRaised()], []),
            ])
        )
        ],
    [],
    )
    );
}