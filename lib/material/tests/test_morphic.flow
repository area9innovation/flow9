import material/material2tropic;

main() {
	opened = make(true);
	animationTick(opened);
	content =
		MCols([
			MHamburgerArrowAnimatedIcon(opened, [MIconSize(100.)]),
			MHamburgerCloseAnimatedIcon(opened, [MIconSize(100.)]),
			MPlayPauseAnimatedIcon(opened, [MIconSize(100.)]),
			// MGroup2(
			// 	MHamburgerArrowAnimatedIcon(opened, [FillOpacity(0.5), MIconSize(100.)]),
			// 	MIcon("menu", [MBlue(900), FillOpacity(0.5), MIconSize(100.)])
			// ),
			// MGroup2(
			// 	MHamburgerCloseAnimatedIcon(opened, [FillOpacity(0.5), MIconSize(100.)]),
			// 	MIcon("menu", [MBlue(900), FillOpacity(0.5), MIconSize(100.)])
			// ),
			// MGroup2(
			// 	MPlayPauseAnimatedIcon(opened, [FillOpacity(0.5), MIconSize(100.)]),
			// 	MIcon("play_arrow", [MBlue(900), FillOpacity(0.5), MIconSize(100.)])
			// ),
		]);
	mrender(makeMaterialManager([]), true, content);
}

animationTick(opened : DynamicBehaviour<bool>) -> void {
	timer(if (slowAnimation) 7000 else 2000, \ -> {
		reverseBehaviour(opened);
		animationTick(opened)
	})
}