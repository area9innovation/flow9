export {
	// Base URL for PHP requests (path to www folder)
	getAppUrl() -> string;
	setAppUrl(url : string) -> void;
}

appUrl = ref "";

getAppUrl() -> string {
	^appUrl
}

setAppUrl(url : string) -> void {
	appUrl := url;
}

