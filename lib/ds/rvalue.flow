export {
	// The key idea is that the data in the children should not be in the value.
	// The children is only used for the recursive parts
	RValue(value : ?, children : [RMember<?>]);
		RMember(key : string, value : RValue<?>);

	// A pointer to a value is a tree path through members
	RPath(path : [string]);
}
