import stylestructs;


/// This file contains useful wrappers for some styles
/// Usually are used in styles array.

export {
	TextStyle : (style : [CharacterStyle]);
	BackgroundStyle : (style : [GraphicsStyle]);
	DrawStyle : (style : [GraphicsStyle]);
	
	Padding : (padding : double);
	CornerRadius : (radius : double);
	Alignment : (xalign : double, yalign : double);
}

