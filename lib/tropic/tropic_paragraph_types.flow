import form/paragraphtypes;
import tropic/tropic_metrics;

export {
	TParagraphElement ::= TWrapElement, TTextFragments;
		
		TTextFragments(parts : List<TWrapElement>);
		
		TWrapElement ::= TTextElement, TInspectElement;
		
			TTextElement ::= TWord, NewLine;
		
			TInspectElement(
				element : TWord,
				index : int,
				point : DynamicBehaviour<Point>,
				size : DynamicBehaviour<WidthHeight>,
				lineHeight : DynamicBehaviour<double>,
				lineNumber : DynamicBehaviour<int>,
				detached : DynamicBehaviour<bool>
			);
	
	TParaWord(
		word : TWrapElement,
		metrics : TFormMetrics,
		ghostView : GhostTropic,
		id : int
	);

	GhostTropic ::= TGhosted, Tropic, TCoordinateInspectElement;
		TCoordinateInspectElement(
			index : int,
			point : DynamicBehaviour<Point>,
			wh : DynamicBehaviour<WidthHeight>,
			lineHeight : DynamicBehaviour<double>,
			form : Tropic,
			detached : DynamicBehaviour<bool>
		);

	TGhosted ::= GhostElem, TInspectGhost;
		GhostElem(point : DynamicBehaviour<Point>, form : Tropic);

		TInspectGhost(
			point : DynamicBehaviour<Point>,
			wh : DynamicBehaviour<WidthHeight>,
			lineHeight : DynamicBehaviour<double>,
			lineNumber : DynamicBehaviour<int>,
			form : Tropic
		);
}