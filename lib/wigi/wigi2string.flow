import wigi/types;
import wigi/storage;

export {
	// Convert this Wigi to string
	wigi2string(w : WigiElement) -> string;
}

wigi2string(wigi : WigiElement) -> string {
	serialize(wigiForStorage(wigi, false))
}