import formats/wiki/wikimath;
import formats/wiki/wikilexer;

export {
	WikiTextElement ::= WikiAns, WikiNoWiki, WikiSmartArt, WikiDecoratedImage, WikiLearningResource, WikiSpan,
				WikiDoc, WikiStory, WikiOneLine, WikiTable, WikiMedia, WikiItalic, WikiBold, WikiBoldItalic,
				WikiSubscript, WikiSuperscript, WikiUnderline, TeXLikeExpression, WikiBaseText, WikiWigi;

		WikiAns : ();
		WikiNoWiki : ();
		WikiSmartArt : ();
		WikiDecoratedImage : ();
		WikiLearningResource : ();
		WikiSpan : ();
		WikiDoc : ();
		WikiStory : ();
		WikiOneLine : ();
		WikiTable : ();
		WikiMedia : ();
		WikiItalic : ();
		WikiBold : ();
		WikiBoldItalic : ();
		WikiSubscript : ();
		WikiSuperscript : ();
		WikiUnderline : ();
		WikiBaseText : ();

	WikiTextElementDescription ::= WikiNestedStruct, WikiFlatStruct, TeXLikeElement;
	//starts and ends with tags. Allows nesting
	WikiNestedStruct(openTag : string, closeTag : string, additionalTags : [string], element : WikiTextElement);
	//Starts and ends with any pair of strings. Does not allow nesting
	WikiFlatStruct(start : string, end : string, element : WikiTextElement);
	// We do not know end of TeXLikeElement additional lookahead is necessary.
	//TeXLikeElement : (representation : string, type : TeXLikeExpression, args : Maybe<WikiArgsDescription>);


}
